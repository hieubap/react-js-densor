import styled from "styled-components";

export const ContainerStyle = styled.div`
  .oneline {
    text-overflow: ellipsis;
    overflow: hidden;
    width: 160px;
    height: 1.2em;
    white-space: nowrap;
    width: 100%;
  }
  .table button {
    padding: 5px 3px;
    margin-right: 5px;
  }
  .now-ui-icons {
    width: 24px;
  }
`;
